---
name:
claim:
tags: []
source_url:
source_title:
accessed: # YYYY-MM-DD, the day the source was read
confidence: medium # high | medium | low
status: unverified # unverified | verified | disputed (you set this after review)
---

<!-- Copy this file to canon/facts/<slug>.md, one per checkable real-world
     fact. The fact locker is the harness's memory of true detail: the name
     of the county form, the exact reinspection fee, the statute number. The
     frontmatter above is the diffable contract -- the research pipeline
     writes new facts here and surfaces conflicts with existing ones for you
     to resolve; it never overwrites. `source_url` is required: a fact with
     no source is dropped, never stored. `status` and everything below the
     frontmatter are yours -- the harness never machine-edits them once you
     have touched them. -->

## Quotes

<!-- Verbatim passage(s) from the source that support the claim. Copy
     exactly so the claim can be checked against the words that back it. -->

## Notes

<!-- Why this matters to the story, caveats, disputes, follow-up to do. -->
